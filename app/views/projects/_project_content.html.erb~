	<div id="project_description" class="prepend-top">
	

	<% if (@page_content = @project.description) %> 
    <% if @project.photo.exists? %>
    <div id="project_picture", align="center">
	<%= image_tag @project.photo.url(:small) %>
	</div>
	<% end %>
    <p><%= raw @page_content %></p>
	<% else %>
    <p><%= raw @page_content %></p>
<% end %>
    </div>
