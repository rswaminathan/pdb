<% if signed_in? && @current_user == User.find_by_id(params[:id])%>
<%= link_to "Add a Project!", new_project_path %>
<% end %>

<%if @user.projects.any? %>
  <div id="project">
    <h2><%= pluralize(@user.projects.count, "Project") %>:</h2>
    <table border="0" cellspacing="5" cellpadding="5">
	<% if !@projects.nil? %>
          <% @projects.each do |p| %>
            <tr>
              <td width="80px;"><%=image_tag p.photo.url(:thumb) %></td>
              <td>
                <div id="project_name_div"><%= link_to p.name, p %></div> 
                <div id="project_description_div"><%= p.description %></div>
                <div id="project_kind"><%= p.kind%></div>
              </td>
                          
            </tr>
          <% end %>
		<% end %>
    </table>
  </div>
<% end %>
