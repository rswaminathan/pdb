<div id='header'>
  <div class="container">
   <div class="span-15"><%=link_to "muddfish", root_url%></div>
   <div class="span-9 last" id="header_menu">
      <% if signed_in? %>
			<%= link_to "Add a Project!", new_project_path %> | <%=link_to "Profile", user_path(current_user) %> | <%= link_to "Log Out", logout_path %>
	    <% else %>
		<%= link_to "Make Your Own Profile!", new_user_path %> | <%= link_to "Sign In", login_path %> 	
    	<% end %>	
   </div>
  </div>
</div>