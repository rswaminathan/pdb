<div id='header'>
  <div class="container">
   <div class="span-15">
<div id="muddfish_title_header"><%=link_to "muddfish", root_url%></div></div>
   <div class="span-9 last" id="header_menu">
      <% if signed_in? %>
			<%= link_to "Add Project!", new_project_path %> | <%=link_to "Profile", user_path(current_user) %> | <%= link_to "Account", edit_user_path(current_user)  %>	| <%= link_to "Log Out", logout_path %>
	    <% else %>

    	<% end %>	
   </div>
  </div>
</div>
