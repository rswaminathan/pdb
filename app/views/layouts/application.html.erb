<!DOCTYPE html>
<html>
<head>
  <title><%=title %></title>
  <%= stylesheet_link_tag 'blueprint/screen', :media => 'screen' %>
  <%= stylesheet_link_tag 'blueprint/print',  :media => 'print' %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
<style type="text/css">
.sidebar_header {
 background-color:#333;
 color: #FFF;
 font-size: 1.5em;
 padding-left: 0.5em;
 }

.user{
  font-size: 1.5em; 
  height: 80px;
  padding-top: 0.5em;
}

#project_summary td{
  background: inherit; 
}

#project_kind{
 	border: 3px solid #A00;
	width: 6em;
	text-align:center;
	margin-right:auto;
	background-color:#333;
	border-radius: 6px;
	-moz-border-radius: 6px;
	color: #FFF;
}

#project_name{
  font-size: 4em;
}

#topbar{
   background-color: #333;
   color: #ffffff;
   font-size: 1em;
   margin-bottom: 1em;
   font-weight: bold;
}

#topbar a{
   color: #ffffff;
}


#header_menu{
   text-align:right;
}
</style>
</head>
<body>
<div id='topbar'>
  <div class="container">
   <div class="span-16"><%=link_to "Home", root_url%></div>
   <div class="span-8 last" id="header_menu">
      <% if signed_in? %>
			<%= link_to "Log Out", logout_path %> | <%=link_to "Profile", user_path(current_user) %> |
      <%= link_to "Add a Project", new_project_path %>
	    <% else %>
			<%= link_to "Log In", login_path %> | <%= link_to "Register", new_user_path %>	
    	<% end %>	
   </div>
  </div>
</div>

<div class="container">
<section class ="round">
	<% flash.each do |key, value| %>
		<div class = "flash <%= key %>"><%= value %></div>
	<% end %>
	<%= yield %>
</section>
</div>
<%#= debug(params) if Rails.env.development? %>
</body>
</html>
