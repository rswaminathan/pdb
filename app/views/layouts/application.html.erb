<!DOCTYPE html>
<html>
<head>
  <title><%=title %></title>
  <%= csrf_meta_tag %>
  <%= render 'layouts/stylesheets' %>
<style type="text/css">
body{
  background: url("/images/bg-noise.png") repeat scroll 0 0 #AAAAAA;  
}

.sidebar_header {
 background-color:#333;
 color: #FFF;
 font-size: 1.5em;
 padding-left: 0.5em;
 }

.user{
  font-size: 1.5em; 
  height: 80px;
  padding-top: 0.5em;
}
a {
  color: #333;
  text-decoration: none;
}

#header a{   
	font-weight: bold;
}
#project_summary td{
  background: inherit; 
}

#login_special{
	color: #660;   
	font-size: 1.3em;	
	margin-top: 1em;
   margin-bottom: 1em;
 	border: 1px solid #D8A800;
	text-align:center;

}
#login_special a{
	color: #B67300;   
	text-align:center;	

}


#profile_div, #user_projects {
    background: url("/images/bg-gradient-large.png") repeat-x scroll 0 -1px rgba(255, 255, 255, 0.1);
    box-shadow: 0 1px 5px rgba(0, 0, 0, 0.3);
    margin-bottom: 2em;
    padding: 1em 0em 1em 0.5em;
}

#project_kind{
 	border: 2px solid #500;
	width: 6em;
	text-align:center;
	margin-right:auto;
	background-color:#eee;
	border-radius: 3px;
	 font-size: 0.72em;
	-moz-border-radius: 2px;
	color: #300;

}

#project_header{
	text-align:center;
	 font-size: 1.2em;
	font-color:#000;
   	color: #ffffff;
}

#project_header li.current a{
    background: url("/images/bg-gradient.png") repeat-x scroll 0 -1px rgba(255, 255, 255, 0.5);
    border-radius: 0.5em 0.5em 0.5em 0.5em;
    box-shadow: 0 0 1px 0 rgba(0, 0, 0, 0.9);
    font-weight: bold;
    padding: 0.714em 2em;
    text-shadow: 0 1px 1px rgba(255, 255, 255, 0.7);
}

#project_header ul{
	 margin: 2em 2em 5em;
   list-style: none outside;
}

#project_header li{
	 float: left;
   margin: 0 1em 0 0;
   
}


#project_name{
  font-size: 3.5em;
}

#project_name_div{
  font-size: 1.2em;
}

#project_name_div a{
  font-size: 1.7em;
}

#project_description_div{
	font-size: 0.8em;
}

#maker_name{
  font-size: 0.8em;
	margin-right: 2em;
}

#header{
   background-color: #333;
   color: #ffffff;
   font-size: 1em;
   margin-bottom: 1em;
   font-weight: bold;
}

#header{
  margin:0 0 2em 0;
}

#header a{
   color: #ffffff;
   font-weight: bold;
}

#footer{
   align: center;
   color: #000000;
   font-size: 0.8em;   
	 margin-top: 3em;
   margin-bottom: 3em;
   font-weight: bold;
}

#footer a{
   color: inherit;
}
#header_menu{
   text-align:right;

}

#add_collaborator{
  text-align: right;
}

#projects {
  float: left;
}
</style>
</head>
<body>
   <%= render 'layouts/header' %>

<div class="container">
<section class ="round">
	<% flash.each do |key, value| %>
		<div class = "flash <%= key %>"><%= value %></div>
	<% end %>
	<%= yield %>
</section>
</div>
<%= render 'layouts/footer' %>
<%= debug(params) if Rails.env.development?%>
</body>
</html>
